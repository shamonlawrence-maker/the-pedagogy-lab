{
  "name": "the-pedagogy-lab-site",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i site/styles/input.css -o assets/site.css --minify",
    "build": "npm run build:css && eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^5.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14"
  }
}
